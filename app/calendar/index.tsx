import { ScreenPlaceholder } from '../../components/ScreenPlaceholder';

export default function CalendarScreen() {
  return (
    <ScreenPlaceholder
      title="Calendar"
      description="Phase 4 will surface weekly call logs, summaries, and navigation here."
    />
  );
}

